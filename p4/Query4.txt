SELECT P.CTYNAME, P.POPESTIMATE2012
FROM POP_EST_STATE_COUNTY P,
	(SELECT (MAX(POPESTIMATE2012))AS POPESTIMATE2012
	FROM POP_EST_STATE_COUNTY
	WHERE SUMLEV = 50
	GROUP BY STNAME) AS R
WHERE P.POPESTIMATE2012 = R.POPESTIMATE2012
GROUP BY STNAME;
